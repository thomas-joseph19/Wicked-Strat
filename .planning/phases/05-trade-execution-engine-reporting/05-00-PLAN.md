---
phase: 05-trade-execution-engine-reporting
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/position.py
  - src/entry.py
  - src/config.py
  - tests/phase05/__init__.py
  - tests/phase05/conftest.py
autonomous: true
requirements:
  - SL-01
  - SL-02
  - SL-03
  - SL-04
  - TP-01
  - TP-02
  - EXEC-01
must_haves:
  truths:
    - "TradeSetup carries TP1 as target_price and explicit tp2_price per TP-01/TP-02"
    - "Stop prices on factories align with SL-01..SL-04 (entry-layer math unchanged; execution consumes stop_price)"
    - "Run output root is injectable for tests (tmp_path); production default resolves to D-15 layout"
  artifacts:
    - path: src/position.py
      provides: "TradeSetup + FillEventKind/FillEvent stubs + ExitType enum skeleton exports"
    - path: src/entry.py
      provides: "build_trade_setup attaches tp2_price per TP-02"
    - path: src/config.py
      provides: "RunPaths or equivalent: run_root, csv_path, charts_dir, run_timestamp string"
    - path: tests/phase05/conftest.py
      provides: "synthetic bars, InstrumentConfig, TradeSetup factory, tmp_path output_root fixture"
  key_links:
    - from: "src/entry.py"
      to: "src/position.py"
      via: "TradeSetup(..., tp2_price=...)"
---

<objective>
Wave 0: **contracts and harness** for Phase 5 — extend `TradeSetup` with **TP2**, define **FillEvent** / exit typing stubs, add **`RunPaths`** (injectable output tree per **D-12–D-15**), and create **`tests/phase05/`** with factories. No full `Position.update` logic here (plans 05-01+).

Purpose: Unblocks parallel implementation; keeps file-ownership clean before `position.py` grows.

Output: Extended dataclasses, `entry.build_trade_setup` populating `tp2_price`, pytest package + conftest.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md (D-12–D-15, locked fills)
@.planning/phases/05-trade-execution-engine-reporting/05-RESEARCH.md (TP-02 gap, RunPaths, schema)
@.planning/REQUIREMENTS.md (SL, TP, EXEC-01)
@src/position.py
@src/entry.py
@src/config.py

<interfaces>
From `src/entry.py` today — SL/TP1 computation (SL-01..SL-04, TP-01):

```python
def compute_sl_tp1_long(lvn, atr5, sp_zones, entry_price) -> tuple[float, float]: ...
def compute_sl_tp1_short(lvn, atr5, sp_zones, entry_price) -> tuple[float, float]: ...
def build_trade_setup(...) -> TradeSetup: ...
```

**TP-02 (implement in this plan):** Next respected SP zone in trade direction beyond TP1; if none within `3 * atr20` price distance, use next valid LVN zone midpoint edge in trade direction (document exact edge rule in code comment). Snap TP2 to `tick_size` like TP1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TradeSetup + event type stubs in position.py</name>
  <files>src/position.py, tests/phase05/__init__.py</files>
  <read_first>
    - `.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md` (D-01, D-04–D-08)
    - `src/position.py`
    - `.planning/research/PITFALLS.md` (P4, C3, C4) — naming only; logic in 05-01
  </read_first>
  <acceptance_criteria>
    - `TradeSetup` includes **`tp2_price: float`** (required non-optional for Phase 5 execution).
    - **`FillEvent`** dataclass (or TypedDict) with fields: `bar_index`, `kind` (literal covering ENTRY, TP1_PARTIAL, TP2, SL, BREAKEVEN, HARD_STOP_LVN, EOD), `price`, `contracts`, `direction`.
    - **`ExitType`** enum: `FULL_TP`, `PARTIAL_TP`, `STOP`, `HARD_STOP`, `EOD`, `TIMEOUT` (EXEC-02 vocabulary).
    - **`TradeResult`** dataclass skeleton: all EXEC-01 fields carried + `tp2_price`, `exit_price_tp1`, `exit_price_tp2`, `exit_type`, `gross_pnl`, `net_pnl`, `total_commission`, `trade_index`, `session_date` (implement aggregation helpers in 05-01; types exist here).
  </acceptance_criteria>
  <action>
    Extend **`TradeSetup`** per **EXEC-01** + **TP-02** field **`tp2_price`** per **D-12/D-14** traceability in results.
    Add **`FillEvent`**, **`ExitType`**, **`TradeResult`** as specified in **05-RESEARCH** (adjust field names to match existing `TradeSetup` naming). Do **not** implement `Position.update` yet.
    Keep **`enter_from_setup`** as `NotImplementedError` until 05-01 unless plan 05-01 removes it — do not delete the aggressive-ledge notification hook pattern.
  </action>
  <verify>
    <automated>python -c "from src.position import TradeSetup, FillEvent, ExitType, TradeResult"</automated>
  </verify>
  <done>Import `TradeSetup`, `FillEvent`, `ExitType`, `TradeResult` from `src.position` without circular import errors; `python -c "from src.position import TradeSetup, FillEvent, ExitType, TradeResult"` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TP-02 + SL contract tests; build_trade_setup wires tp2</name>
  <files>src/entry.py, tests/phase05/conftest.py, tests/phase05/test_tp2_and_sl_contract.py</files>
  <read_first>
    - `src/entry.py` (`compute_sl_tp1_*`, `build_trade_setup`, SP zone attributes)
    - `.planning/REQUIREMENTS.md` (TP-02, SL-01..SL-04)
  </read_first>
  <behavior>
    - Long: with two SP zones above entry, TP1 = nearer zone edge, TP2 = next zone edge per TP-02.
    - Short: mirror below entry.
    - Fallback: when no second SP within 3×ATR20, TP2 resolves to chosen LVN edge (document in test name).
    - Factory setup: `stop_price` distance satisfies ≥4 ticks and ≤1.5×ATR20 when factory parameters are chosen (SL-03, SL-04).
  </behavior>
  <acceptance_criteria>
    - **`build_trade_setup`** passes **`tp2_price`** into **`TradeSetup`** (TP-02).
    - New tests **`tests/phase05/test_tp2_and_sl_contract.py`** (or split files) assert numeric TP2 selection and SL distance bounds on synthetic SP/LVN lists.
    - **SL-01..SL-04:** Tests use `compute_sl_tp1_*` outputs; assert tick snap and min/max tick distance vs `atr20` and `tick_size` (indirect validation that execution will consume correct `stop_price`).
  </acceptance_criteria>
  <action>
    Implement **`compute_tp2_*`** helpers in **`src/entry.py`** (or `src/position.py` if import cycles force — prefer **entry** next to TP1 logic). Call from **`build_trade_setup`** so every emitted setup has valid **TP1** (**TP-01** / `target_price`) and **TP-02** (**tp2_price**).
    Create **`tests/phase05/conftest.py`** with minimal factories (bars, `TradeSetup`, `InstrumentConfig`) required by **`test_tp2_and_sl_contract.py`**; extend in later plans as needed.
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_tp2_and_sl_contract.py -q --tb=short -x</automated>
  </verify>
  <done>All new tests green; `build_trade_setup` returns `TradeSetup` with both `target_price` and `tp2_price` populated on constructed scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: RunPaths + injectable output root (D-12–D-15)</name>
  <files>src/config.py, tests/phase05/test_run_paths.py</files>
  <read_first>
    - `.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md` (D-12–D-15)
    - `src/config.py` (`AppConfig.output_dir`)
    - `config.yaml` if present (repo root)
  </read_first>
  <acceptance_criteria>
    - **`RunPaths`** (frozen dataclass or small class) exposes: `run_root: Path`, `csv_path: Path`, `charts_dir: Path`, `run_timestamp: str` where:
      - `run_root = base / f"run_{run_timestamp}"` with `run_timestamp` = `YYYYMMDD_HHMMSS` local time string used consistently in filenames.
      - `csv_path = run_root / f"backtest_results_{run_timestamp}.csv"` (**D-14**).
      - `charts_dir = run_root / f"charts_{run_timestamp}"` (**D-14**).
    - **`make_run_paths(output_base: Path, run_ts: str | None = None) -> RunPaths`** allows tests to pass `tmp_path` and optional fixed timestamp for deterministic assertions.
    - Default **`output_base`** when building from `AppConfig` uses **`AppConfig.output_dir`** (document: on dev machine YAML should point at `D:\Algorithms\Wicked Backtest Results` per **D-15** — do not hardcode `D:\` in library code; YAML/env only).
  </acceptance_criteria>
  <action>
    Add **`RunPaths`** + factory to **`src/config.py`** (or new **`src/run_paths.py`** if `config.py` becomes crowded — single import path for executors).
    Add **`tests/phase05/test_run_paths.py`**: assert path strings match **D-14** pattern given a fixed `run_ts`. Extend **`tests/phase05/conftest.py`** with an `output_root` / `run_paths` fixture if useful for downstream plans.
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_run_paths.py -q --tb=short -x</automated>
  </verify>
  <done>`RunPaths` usable from tests without touching `D:\`; production uses config-driven base.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase05/ -q` passes; `TradeSetup` has `tp2_price`; `RunPaths` matches D-14 segment names.
</verification>

<success_criteria>
- **EXEC-01** satisfied with **tp2_price** field.
- **TP-01 / TP-02** covered by construction + tests.
- **SL-01..SL-04** re-validated via factory tests tied to entry-layer math.
- **D-14** path segments implemented in **`RunPaths`**.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-execution-engine-reporting/05-00-SUMMARY.md`
</output>
