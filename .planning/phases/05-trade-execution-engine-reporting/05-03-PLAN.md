---
phase: 05-trade-execution-engine-reporting
plan: 03
type: execute
wave: 4
depends_on:
  - 05-02
files_modified:
  - pyproject.toml
  - src/position.py
  - src/plotting.py
  - src/reporting.py
  - src/backtest.py
  - tests/phase05/test_plotting_smoke.py
  - tests/phase05/test_reporting_csv_html.py
  - tests/phase05/test_summary_stub.py
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
  - REPORT-04
  - REPORT-05
must_haves:
  truths:
    - "Per-trade Plotly HTML under charts_{run_timestamp}/ with D-14 filename pattern (supersedes REPORT-02 trade_NNN)"
    - "CSV backtest_results_{run_timestamp}.csv appends one row per closed trade with headers-once (D-12, D-13, REPORT-03, REPORT-05)"
    - "summary.md stub with run metadata + trade count + total net PnL (REPORT-04 minimal scope)"
  artifacts:
    - path: src/plotting.py
      provides: "build_trade_chart(...) -> go.Figure per D-09–D-11"
    - path: src/reporting.py
      provides: "RunWriter: append_trade_csv, write_chart_html, finalize_summary"
    - path: src/backtest.py
      provides: "Session/bar loop hooking Position + RunWriter + RunPaths"
  key_links:
    - from: "src/reporting.py"
      to: "RunPaths"
      via: "paths.csv_path / paths.charts_dir"
---

<objective>
Deliver **REPORT-01..05**: **Plotly** candlestick + SVP + overlays (**D-09–D-11**), **on-disk layout** matching **D-14–D-15** (authoritative over REPORT-02 literal `trade_NNN.html`), **incremental CSV** (**D-12–D-13**), **`summary.md` stub** (**REPORT-04**), and a minimal **`backtest.py` orchestrator** that ties **`RunPaths`**, **`Position`**, and writers together using **injectable `output_base`**.

Purpose: Phase 5 produces inspectable artifacts after each trade without waiting for Phase 6 metrics.

Output: `src/plotting.py`, `src/reporting.py`, real `src/backtest.py` entry (replace stub), pytest with **tmp_path** (no `D:\` assertions in CI).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md (D-09–D-15)
@.planning/phases/05-trade-execution-engine-reporting/05-RESEARCH.md (Plotly subplots, output layout)
@src/config.py (`RunPaths`, `AppConfig`)
@src/position.py (`TradeResult`, `TradeSetup`)
@src/volume_profile.py (if `VolumeProfile` type exists for SVP — else document tuple contract)

<interfaces>
**Chart window (D-09):** From **`TradeResult`**, take entry/exit bar indices (executor must ensure **`TradeResult`** carries **`entry_bar_index`** / **`exit_bar_index`** or equivalent — add fields in **05-01/05-02** if missing). Slice **session** bar DataFrame to **`[entry_idx - 60, exit_idx + 30]`** intersected with session min/max indices.

**Filename (D-14):** `{trade_index:04d}_{session_date}_{direction}_{setup_type}.html` — normalize `setup_type` to string without spaces (e.g. `THREE_STEP`).

**Title (D-11):** `{instrument} | {date} | {direction} | {setup_type} | PnL: ${net_pnl:.2f}` (use **net** realized).

**SVP (D-10, discretion):** `plotly.subplots.make_subplots(rows=1, cols=2, shared_yaxes=True, column_widths=[0.72, 0.28])` — left candlesticks vs time, right **`go.Bar(orientation='h')`** volume-at-price aligned to price scale.

**CSV:** `csv.DictWriter`, **`newline=""`**, append mode, write header iff file does not exist (**D-13**). Columns = **`TradeResult`** / **`TradeSetup`** superset needed for ML later (document field list in module docstring).

**REPORT-02:** Implement **run folder** `run_YYYYMMDD_HHMMSS` under configurable base (**D-15**); **do not** use legacy `charts/trade_NNN.html` path — treat REPORT-02 as “per-trade HTML under run tree” satisfied by **D-14**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: plotting.py — candlestick, SVP, overlays (REPORT-01, D-09–D-11)</name>
  <files>pyproject.toml, src/plotting.py, src/position.py, tests/phase05/test_plotting_smoke.py</files>
  <read_first>
    - `.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md` (D-09, D-10, D-11)
    - `src/plotting.py` (stub)
    - Official Plotly docs (subplots + candlestick) if local API unclear
  </read_first>
  <acceptance_criteria>
    - **`build_trade_chart(trade, bars_df, volume_profile, lvn_zone) -> plotly.graph_objects.Figure`** exists.
    - Figure includes: **candlestick**; **hlines** entry=blue, SL=red, TP1=green dashed, TP2=green solid; **yellow LVN rect** across x-range; **vertical dashed markers** at fill event times (pass list of event timestamps or indices).
    - **SVP** panel present per discretion (**go.Bar** horizontal).
    - Title matches **D-11** pattern.
    - `fig.to_html()` runs without error.
  </acceptance_criteria>
  <action>
    Replace **`plotting.py`** stub. Add **`plotly`** to **`pyproject.toml`** under **`dependencies`** (or **`optional-dependencies.dev`** if maintainers prefer — **prefer main deps** so `main.py` backtest works without extras).

    If **`TradeResult`** lacks bar indices for windowing, add **`entry_bar_index`** and **`exit_bar_index`** (and ensure **`Position`** / builder sets them) — minimal delta in **`src/position.py`** acceptable in this plan’s file list (include **`src/position.py`** in **`files_modified`** if touched).
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_plotting_smoke.py -q --tb=short -x</automated>
  </verify>
  <done>Smoke test asserts trace types (candlestick present, minimum shape count) and `to_html` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reporting.py — incremental CSV + HTML paths (REPORT-03, REPORT-05, D-12–D-14)</name>
  <files>src/reporting.py, tests/phase05/test_reporting_csv_html.py</files>
  <read_first>
    - `.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md` (D-12–D-14)
    - `src/config.py` (`RunPaths`)
  </read_first>
  <behavior>
    - First `append_trade_row`: creates parent dirs, writes header + row.
    - Second call: no header, one appended row.
    - Chart path: `run_root / f"charts_{ts}" / f"{idx}_{date}_{dir}_{stype}.html"`.
  </behavior>
  <acceptance_criteria>
    - **`RunWriter`** class with **`append_trade_csv(trade: TradeResult)`**, **`write_trade_html(trade, fig)`**, in-memory **`rows: list[dict]`** mirror for summary (**D-13** secondary buffer).
    - Tests use **`tmp_path`** only.
  </acceptance_criteria>
  <action>
    Create **`src/reporting.py`**. Use **`Path.mkdir(parents=True, exist_ok=True)`** before writes.

    **REPORT-02** satisfied by **`RunPaths.run_root`** + **`charts_dir`** layout under injected base (document in module docstring).
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_reporting_csv_html.py -q --tb=short -x</automated>
  </verify>
  <done>CSV row count and HTML filename regex match **D-14**.</done>
</task>

<task type="auto">
  <name>Task 3: summary.md stub + backtest.py orchestration (REPORT-04, integration)</name>
  <files>src/reporting.py, src/backtest.py, tests/phase05/test_summary_stub.py</files>
  <read_first>
    - `src/backtest.py` (stub)
    - `.planning/PROJECT.md` (output tree — align wording with D-14)
    - `src/position.py` (`Position`, `TradeResult`)
  </read_first>
  <acceptance_criteria>
    - **`RunWriter.finalize_summary()`** writes **`summary.md`** under **`run_root`** with: run timestamp, instrument symbol, trade count, sum of **net_pnl** from buffer, path to CSV relative to run root.
    - **`src/backtest.py`** exposes a **`run_session_backtest(...)`** or **`BacktestRunner`** that: builds **`RunPaths`** from config + optional override; on each **CLOSED** trade, builds **`TradeResult`**, calls **`append_trade_csv`**, builds figure via **`build_trade_chart`**, **`write_trade_html`** — **per trade** (**D-12**, **REPORT-05**).
    - **`tests/phase05/test_summary_stub.py`** validates **`summary.md`** exists and contains numeric total after synthetic trades.
  </acceptance_criteria>
  <action>
    Replace **`backtest.py`** stub with minimal loop: accept pre-sliced session bars + list of setups (or single setup stream from Phase 4 — **interface at Claude discretion**: function signature documented in docstring). Focus on **wiring**, not full 12-year harness (Phase 1–4 integration can be thin mock in test).

    **REPORT-04:** Explicitly **not** full institutional metrics (Phase 6); header in **`summary.md`** stating “Phase 5 stub — metrics in Phase 6”.
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_summary_stub.py tests/phase05/test_reporting_csv_html.py -q --tb=short -x</automated>
  </verify>
  <done>End-to-end writer path green on tmp_path.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase05/ -q` and `python -m pytest tests/ -q` (full suite).
</verification>

<success_criteria>
- **REPORT-01..05** addressed: interactive chart, correct paths (**D-14/D-15** semantics), incremental CSV, summary stub, per-trade persistence.
- **D-09–D-11** visual contract implemented (automated smoke; optional human browser check not gated).
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-execution-engine-reporting/05-03-SUMMARY.md`
</output>
