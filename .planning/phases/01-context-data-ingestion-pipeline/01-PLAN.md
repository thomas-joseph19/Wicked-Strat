---
wave: 1
depends_on: []
files_modified: ["src/data/ingestion.py", "requirements.txt"]
autonomous: true
requirements_addressed: ["Req 1.1", "Req 1.2", "Req 1.3"]
---

# Plan 1: Parquet Ingestion and Data Normalization

<objective>
Build the `ingestion.py` module handling the dataset import, explicit float-integer transformations to prevent floating-point tick errors, and custom timezone logic slicing the chronological array into explicit 6:00 PM ET trading sessions.
</objective>

<task>
<read_first>
- `nq_1min_10y.parquet` (For determining exact column structures, use Python shell output to verify if needed).
- `c:/Users/thoma/Trading/Wicked Strat/.planning/REQUIREMENTS.md` (Specifically Requirement 1 list).
</read_first>
<action>
1. Create `requirements.txt` with `polars>=1.0.0` and `pandas>=2.0.0` and `pyarrow>=15.0.0`.
2. Create `src/data/ingestion.py` containing the `IngestionPipeline` class.
3. Write `def load_parquet(self, path: str) -> pl.DataFrame:` utilizing Polars `read_parquet`. Ensure timezone is set to `America/New_York`.
4. Write `def apply_integer_cast(self, df: pl.DataFrame, multiplier: int = 100) -> pl.DataFrame:`. Add columns `open_int`, `high_int`, `low_int`, `close_int` generated by multiplying the standard columns by 100 and casting to Int32. Add this safely so no original data is mutated/lost.
5. Write `def compute_session_bounds(self, df: pl.DataFrame) -> pl.DataFrame:`. Add a `session_date` feature. The logic: If time is `>= 18:00:00 ET`, it belongs to the *next* calendar day's session. Treat all data prior to 18:00 as the *current* calendar day's session.
</action>
<acceptance_criteria>
- `requirements.txt` contains `polars`, `pandas`, `pyarrow`.
- `src/data/ingestion.py` defines `IngestionPipeline`.
- `apply_integer_cast` creates fields like `close_int`.
- `compute_session_bounds` exists and correctly shifts sessions over the 18:00 ET boundary.
</acceptance_criteria>
</task>

## Verification
- Can initialize `IngestionPipeline` and call `load_parquet`.
- `df.dtypes` confirms presence of `Int32` or `Int64` type columns matching `_int` suffixes.
- Head/Tail tests showing 17:59 mapping to Day X, and 18:00 mapping to Day X+1.

## Must Haves
- Timezone rules strictly follow standard US ET implementations.
- No string-based date parsing loops; ensure fast vectorized operations via native Polars logical conditions / shifts.
