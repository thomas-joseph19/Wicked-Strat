---
phase: 07-trade-logic-audit-verification
plan: 03
type: execute
wave: 4
depends_on:
  - 07-02
files_modified:
  - src/plotting.py
  - src/backtest.py
  - tests/phase07/test_dual_chart_smoke.py
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "Dual-panel NQ+ES candlesticks shared x-axis; same window as Phase 5 D-09"
    - "Optional confluence overlays v1: TPO bias text + SP rectangles; ISMT/SMT stubs documented if data missing"
  artifacts:
    - path: src/plotting.py
      provides: "build_trade_chart_dual(trade, bars_df, volume_profile, lvn_zone, chart_ctx | None) -> go.Figure"
  key_links:
    - from: "build_trade_chart_dual"
      to: "bars_df open_es/high_es/low_es/close_es"
      via: "second row candlestick"
---

<objective>
Implement **07-CONTEXT D-07–D-09**: **per-trade** charts with **NQ primary** + **ES** secondary panel, **shared time axis**, and **incremental confluence overlays** (bias label, SP zones; ISMT/SMT markers when **`chart_ctx`** supplies coordinates).

Purpose: Visual verification of **SMT/NQ–ES** relationship without replacing Phase 5 chart for all users.

Output: **`build_trade_chart_dual`** + optional **`TradeChartContext`** dataclass in **`src/plotting.py`** or **`src/chart_context.py`**; **`run_session_backtest`** flag **`use_dual_charts: bool = False`**; smoke test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md (D-07, D-08, D-09)
@src/plotting.py (`build_trade_chart`, `_window_slice`)
@src/backtest.py (`write_trade_html`)
@src/position.py (`TradeResult`)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TradeChartContext + dual layout</name>
  <files>src/plotting.py, tests/phase07/test_dual_chart_smoke.py</files>
  <read_first>
    - `src/plotting.py` (full file)
    - `.planning/phases/05-trade-execution-engine-reporting/05-CONTEXT.md` (D-09 window)
  </read_first>
  <acceptance_criteria>
    - **`TradeChartContext`** dataclass (in **`plotting.py`** or **`src/chart_context.py`**) with optional fields: **`bias: str | None`**, **`upper_ratio: float | None`**, **`sp_zones: Sequence[Any] | None`**, **`structural_note: str | None`** — all optional for backward compatibility.
    - **`build_trade_chart_dual(..., chart_ctx: TradeChartContext | None = None)`** uses **`make_subplots(rows=2, cols=2, row_heights=[0.7, 0.3], shared_yaxes=False, shared_xaxes=True)`** or equivalent with **NQ** candlestick **row 1 col 1**, **ES** candlestick **row 2 col 1**, **SVP** may remain **col 2** spanning rows **or** NQ-only SVP per **executor discretion** (document in docstring).
    - **`fig.to_html()`** succeeds on synthetic **`bars_df`** with both **`open_nq`** and **`open_es`** columns.
    - `grep -n "def build_trade_chart_dual" src/plotting.py` matches.
  </acceptance_criteria>
  <action>
    Reuse **`_window_slice`** for both instruments.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_dual_chart_smoke.py -k dual -q --tb=short</automated>
  </verify>
  <done>Dual layout smoke passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlays v1 (bias + SP)</name>
  <files>src/plotting.py, tests/phase07/test_dual_chart_smoke.py</files>
  <read_first>
    - `.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md` (D-08)
  </read_first>
  <acceptance_criteria>
    - When **`chart_ctx.bias`** set, **`fig.add_annotation`** (or title suffix) shows **`BIAS: {bias}`** on **NQ** panel.
    - When **`chart_ctx.sp_zones`** non-empty, **≥ 1** green **`add_shape`** `rect` spans x-range (same as LVN yellow pattern).
    - `grep -n "sp_zones" src/plotting.py` matches at least twice.
  </acceptance_criteria>
  <action>
    SP zone objects assumed **`.low` / `.high`** like **`LVNZone`**; document in docstring.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_dual_chart_smoke.py -k overlay -q --tb=short</automated>
  </verify>
  <done>Overlay smoke passes.</done>
</task>

<task type="auto">
  <name>Task 3: backtest flag</name>
  <files>src/backtest.py, tests/phase07/test_dual_chart_smoke.py</files>
  <read_first>
    - `src/backtest.py` (`write_trade_html`, `build_trade_chart`)
  </read_first>
  <acceptance_criteria>
    - **`run_session_backtest(..., use_dual_charts: bool = False, chart_ctx: TradeChartContext | None = None)`** when **`True`** calls **`build_trade_chart_dual`** instead of **`build_trade_chart`** for **`write_trade_html`**.
    - Default **`False`** preserves Phase 5 behavior (**`python -m pytest tests/phase05/`** still green).
    - `grep -n "use_dual_charts" src/backtest.py` matches.
  </acceptance_criteria>
  <action>
    Import **`TradeChartContext`** from **`plotting`** (or chart_context module).
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/ tests/phase07/test_dual_chart_smoke.py -q --tb=short</automated>
  </verify>
  <done>Integration smoke green.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase07/test_dual_chart_smoke.py tests/phase05/ -q`
</verification>

<success_criteria>
- D-07–D-09 baseline: dual candles + optional overlays + opt-in backtest wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trade-logic-audit-verification/07-03-SUMMARY.md`
</output>
