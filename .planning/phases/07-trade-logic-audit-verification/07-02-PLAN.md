---
phase: 07-trade-logic-audit-verification
plan: 02
type: execute
wave: 3
depends_on:
  - 07-01
files_modified:
  - src/plotting.py
  - src/metrics.py
  - tests/phase07/test_equity_curve_chart.py
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "Plotly HTML equity_curve_{run_timestamp}.html with line + drawdown shading + $100k line + top-3 DD annotations"
  artifacts:
    - path: src/plotting.py
      provides: "build_equity_curve_figure(dates, equity, episodes) -> go.Figure"
    - path: src/metrics.py
      provides: "optional thin helper export if needed"
  key_links:
    - from: "build_equity_curve_figure"
      to: "list_drawdown_episodes"
      via: "annotation labels"
---

<objective>
Deliver **07-CONTEXT D-05/D-06**: **compounded equity** visualization with **drawdown fill vs running peak**, **$100k** reference line, and **top 3** drawdown annotations from **`metrics.list_drawdown_episodes`**.

Purpose: Visual audit artifact alongside **`summary.md`** for M1 sign-off.

Output: **`build_equity_curve_figure`** + **`write_equity_curve_html(run_root, run_timestamp, fig)`** (or equivalent on **`RunPaths`**) + pytest smoke.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md (D-05, D-06)
@src/metrics.py (`compounded_equity_from_returns`, `list_drawdown_episodes`, `build_daily_pnl_and_returns`)
@src/plotting.py (Plotly patterns)
@src/config.py (`RunPaths`)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_equity_curve_figure</name>
  <files>src/plotting.py, tests/phase07/test_equity_curve_chart.py</files>
  <read_first>
    - `src/plotting.py` (`build_trade_chart` layout)
    - `src/metrics.py` (`list_drawdown_episodes`)
  </read_first>
  <acceptance_criteria>
    - **`build_equity_curve_figure(dates: Sequence[str], equity: pd.Series, top_episodes: list[dict] | None = None) -> go.Figure`** includes:
      - **Scatter/line** equity vs date
      - **Horizontal line** `y=100_000` (or parameterized **`start_equity`**)
      - **Filled area** for drawdown (equity vs **`cummax(equity)`**) with **red** low-opacity fill
      - **≥ 1** `layout.Annotation` when **`top_episodes`** non-empty (top 3 peak→trough labels)
    - `grep -n "def build_equity_curve_figure" src/plotting.py` matches.
  </acceptance_criteria>
  <action>
    Parse **`dates`** to datetime for x-axis when strings are ISO **YYYY-MM-DD**.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_equity_curve_chart.py -k figure -q --tb=short</automated>
  </verify>
  <done>Figure structure test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write file D-06 naming</name>
  <files>src/plotting.py, tests/phase07/test_equity_curve_chart.py</files>
  <read_first>
    - `.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md` (D-06)
  </read_first>
  <acceptance_criteria>
    - **`write_equity_curve_html(run_root: Path, run_timestamp: str, fig: go.Figure) -> Path`** writes **`run_root / f"equity_curve_{run_timestamp}.html"`** with **`fig.write_html(..., include_plotlyjs='cdn')`**.
    - Test **`tmp_path`**: file exists, size **> 500** bytes.
    - `grep -n "equity_curve_" src/plotting.py` matches.
  </acceptance_criteria>
  <action>
    Optionally call from **`compute_and_write_institutional_summaries`** behind flag **`write_equity_curve: bool = False`** — **planner discretion** in **07-03** if not done here.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_equity_curve_chart.py -k write_equity -q --tb=short</automated>
  </verify>
  <done>Write test passes.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase07/test_equity_curve_chart.py -q`
</verification>

<success_criteria>
- D-05/D-06 equity artifact reproducible from tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trade-logic-audit-verification/07-02-SUMMARY.md`
</output>
