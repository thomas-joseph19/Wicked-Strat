---
phase: 07-trade-logic-audit-verification
plan: 01
type: execute
wave: 2
depends_on:
  - 07-00
files_modified:
  - src/audit.py
  - tests/phase07/test_audit_cli.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
must_haves:
  truths:
    - "audit_report.md rows for AUDIT-01..07 with PASS/FAIL/MANUAL/SKIP and timestamps"
    - "AUDIT-01 documented as manual two-run diff; canonical CSV path stated"
  artifacts:
    - path: src/audit.py
      provides: "run_checklist(), write_audit_report(run_root | Path)"
  key_links:
    - from: "write_audit_report"
      to: "RunPaths.run_root / audit_report.md"
      via: "optional CLI --output"
---

<objective>
Implement **`src/audit.py`** per **07-CONTEXT D-04**: programmatic checklist runner that writes **`audit_report.md`** with one row per **AUDIT-01..07**, evidence strings, and timestamps.

Purpose: Single command to regenerate the audit trail after code changes; **AUDIT-01** remains **MANUAL** per **D-01/D-02** with copy-paste instructions.

Output: `src/audit.py` + thin pytest invoking report generation on **`tmp_path`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md (D-01, D-02, D-04)
@.planning/REQUIREMENTS.md (AUDIT-01..07)
@src/config.py (`RunPaths`, `make_run_paths`)
@.planning/PROJECT.md (output paths)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit row model + writer</name>
  <files>src/audit.py, tests/phase07/test_audit_cli.py</files>
  <read_first>
    - `.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md` (D-04)
    - `src/config.py` (`make_run_paths`)
  </read_first>
  <acceptance_criteria>
    - **`src/audit.py`** defines **`AuditRow`** (dataclass or TypedDict) with **`audit_id`**, **`description`**, **`result`** (`PASS`|`FAIL`|`SKIP`|`MANUAL`), **`evidence`**, **`checked_at`** (ISO-8601 string).
    - **`write_audit_report(path: Path, rows: list[AuditRow]) -> None`** writes Markdown table with header **`| ID | Description | Result | Evidence | Checked |`**.
    - `grep -n "def write_audit_report" src/audit.py` matches.
  </acceptance_criteria>
  <action>
    Use **`path.parent.mkdir(parents=True, exist_ok=True)`** before write.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_cli.py -k write_report -q --tb=short</automated>
  </verify>
  <done>Writer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Programmatic results for AUDIT-02..07</name>
  <files>src/audit.py, tests/phase07/test_audit_cli.py</files>
  <read_first>
    - `tests/phase07/test_audit_regression.py` (reuse same predicates as functions)
    - `src/audit.py` (skeleton)
  </read_first>
  <acceptance_criteria>
    - **`run_programmatic_checks() -> list[AuditRow]`** returns **6** rows (**AUDIT-02**..**AUDIT-07**) with **`PASS`** when invariants hold (delegate to small **pure check functions** imported or duplicated from tests — **no subprocess pytest** required for default path).
    - On deliberate invariant break in test only, inject monkeypatch — optional.
    - `grep -n "def run_programmatic_checks" src/audit.py` matches.
  </acceptance_criteria>
  <action>
    Extract shared predicates into **`src/audit_checks.py`** only if **`audit.py`** exceeds **300 lines**; otherwise keep in **`audit.py`**.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_cli.py -k programmatic -q --tb=short</automated>
  </verify>
  <done>Programmatic rows PASS.</done>
</task>

<task type="auto">
  <name>Task 3: AUDIT-01 manual block + CLI entry</name>
  <files>src/audit.py, tests/phase07/test_audit_cli.py</files>
  <read_first>
    - `.planning/ROADMAP.md` (Phase 7 AUDIT-01)
    - `main.py` (current backtest entry command)
  </read_first>
  <acceptance_criteria>
    - **`run_full_checklist() -> list[AuditRow]`** prepends **AUDIT-01** with **`MANUAL`** and **`evidence`** containing: (1) canonical trade log path = **`RunPaths.csv_path.name`** pattern **`backtest_results_*.csv`** **or** explicit **`trades.csv`** per **executor lock**; (2) example **`fc /b file1 file2`** (Windows) **and** `diff` (Unix) one-liner; (3) **two** identical CLI invocations placeholder **`python main.py --mode backtest --start ... --end ...`**.
    - **`if __name__ == "__main__"`** block writes **`audit_report.md`** to **`cwd`** or **`--out`** arg.
    - `grep -n "AUDIT-01" src/audit.py` matches.
  </acceptance_criteria>
  <action>
    Default output path: **`./audit_report.md`** when no **`--out`**.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_cli.py -k full_checklist -q --tb=short</automated>
  </verify>
  <done>CLI + MANUAL row test green.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase07/test_audit_cli.py -q` and `python src/audit.py --out tmp_audit.md` (smoke)
</verification>

<success_criteria>
- **D-04** satisfied: machine-generated **`audit_report.md`** with all seven IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trade-logic-audit-verification/07-01-SUMMARY.md`
</output>
