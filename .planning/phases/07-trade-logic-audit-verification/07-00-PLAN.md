---
phase: 07-trade-logic-audit-verification
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entry.py
  - tests/phase07/__init__.py
  - tests/phase07/conftest.py
  - tests/phase07/test_audit_regression.py
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
must_haves:
  truths:
    - "Permanent pytest guards for AUDIT-02,03,04,05,06,07 with synthetic data"
    - "entry==stop rejected before Position (no divide-by-zero, no crash)"
  artifacts:
    - path: tests/phase07/test_audit_regression.py
      provides: "Named tests mapping to AUDIT IDs in docstrings"
  key_links:
    - from: "validate_pre_entry"
      to: "AUDIT-02"
      via: "risk/reward and risk_ticks guards"
---

<objective>
Lock **AUDIT-02 through AUDIT-07** as **always-on regression tests** in **`tests/phase07/`**: RR guard, short P&L polarity, rolling-peak drawdown sanity, TPO resample labels, commission event counts, exact exit fill prices.

Purpose: Every `pytest` run proves the audit checklist items that do not require two full parquet backtests.

Output: `tests/phase07/test_audit_regression.py` (+ minimal `conftest`), and **`src/entry.py`** (or execution path) guard for **AUDIT-02** if not already enforced.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-trade-logic-audit-verification/07-CONTEXT.md (D-03)
@.planning/REQUIREMENTS.md (AUDIT-02..07)
@src/entry.py (`validate_pre_entry`, `build_trade_setup`)
@src/position.py (`Position`, fill prices)
@src/metrics.py (`max_drawdown_stats`)
@src/tpo.py (`compute_tpo_bias` resample)
@tests/phase05/ (reuse patterns)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AUDIT-02 entry == stop guard</name>
  <files>src/entry.py, tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/entry.py` (`validate_pre_entry`, `build_trade_setup`)
    - `.planning/REQUIREMENTS.md` (AUDIT-02)
  </read_first>
  <acceptance_criteria>
    - When **`entry_price == stop_price`**, **`validate_pre_entry(...)`** returns **`False`** OR **`build_trade_setup`** is never called for such a case — document which layer owns the guard.
    - **`tests/phase07/test_audit_regression.py`** contains **`test_audit_02`** (or docstring **`AUDIT-02`**) asserting guard with **`entry_price=stop_price=100.0`** and other valid fields.
    - `grep -n "AUDIT-02" tests/phase07/test_audit_regression.py` matches.
  </acceptance_criteria>
  <action>
    If guard exists only in **`validate_pre_entry`**, add test only. If missing, add **`if abs(entry_price - stop_price) < 1e-9: return False`** (or tick-epsilon) in **`validate_pre_entry`** per spec.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_02 -q --tb=short</automated>
  </verify>
  <done>AUDIT-02 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUDIT-03 short P&L polarity</name>
  <files>tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/position.py` (`fills_to_gross_pnl`, `TradeResult`)
    - `.planning/research/PITFALLS.md` (P4)
  </read_first>
  <acceptance_criteria>
    - Test **`test_audit_03_short_polarity`**: synthetic **`TradeResult`** or **`FillEvent`** list for **SHORT** with known **`net_pnl`** sign vs price direction (price fell → positive net as per engine convention).
    - `grep -n "AUDIT-03" tests/phase07/test_audit_regression.py` matches.
  </acceptance_criteria>
  <action>
    Prefer **`build_trade_result`** + **`fills_to_gross_pnl`** if simpler than full **`Position.update`**.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_03 -q --tb=short</automated>
  </verify>
  <done>AUDIT-03 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AUDIT-04 drawdown rolling peak</name>
  <files>tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/metrics.py` (`max_drawdown_stats`)
    - `tests/phase06/test_daily_equity_drawdown_pf.py`
  </read_first>
  <acceptance_criteria>
    - **`test_audit_04`**: calls **`max_drawdown_stats`** on a non-monotonic equity series; asserts **`max_drawdown_frac <= 0`** and **`max_drawdown_dollars >= 0`** (loss vs peak).
    - `grep -n "AUDIT-04" tests/phase07/test_audit_regression.py` matches.
  </acceptance_criteria>
  <action>
    Thin wrapper acceptable if **`phase06`** already proves math — this test is the **audit traceability** anchor.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_04 -q --tb=short</automated>
  </verify>
  <done>AUDIT-04 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: AUDIT-05 TPO resample labels</name>
  <files>tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/tpo.py` (full `resample` call)
  </read_first>
  <acceptance_criteria>
    - **`test_audit_05`**: constructs synthetic **1-min** bars for one session; asserts **`label='left'`** and **`closed='left'`** appear in **`tpo.py`** via **`inspect.getsource(compute_tpo_bias)`** **OR** behavioral check (first 30m bucket end aligns with spec comment).
    - Preferred: **behavioral** — known timestamps → expected number of TPO bars and first bucket left edge.
    - `grep -n "AUDIT-05" tests/phase07/test_audit_regression.py` matches.
  </acceptance_criteria>
  <action>
    If behavioral test is brittle, use **`grep -n "label='left'" src/tpo.py`** in **`acceptance_criteria`** satisfied by CI + one minimal aggregation assertion.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_05 -q --tb=short</automated>
  </verify>
  <done>AUDIT-05 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: AUDIT-06 commission events</name>
  <files>tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/position.py` (`FillEvent`, `Position.update`)
    - `tests/phase05/test_pnl_commission.py`
  </read_first>
  <acceptance_criteria>
    - **`test_audit_06_tp_path`**: full TP1+TP2 path → **`len(fills) == 4`** with kinds **ENTRY**, **TP1_PARTIAL**, **TP2** (and no spurious fourth exit). ROADMAP “3 commission events” = **three billable fills after entry** → assert **`len([f for f in fills if f.kind != 'ENTRY']) == 3`** OR equivalent documented in test comment.
    - **`test_audit_06_stop_path`**: stop-out → **`len(fills) == 2`** (**ENTRY** + **SL**); post-entry billable count **1**.
    - `grep -n "AUDIT-06" tests/phase07/test_audit_regression.py` matches at least twice.
  </acceptance_criteria>
  <action>
    Cross-check **`.planning/ROADMAP.md`** Phase 7 item 5 and **`tests/phase05/test_pnl_commission.py`**; document any intentional interpretation in the test docstring.
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_06 -q --tb=short</automated>
  </verify>
  <done>AUDIT-06 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: AUDIT-07 exact exit prices</name>
  <files>tests/phase07/test_audit_regression.py</files>
  <read_first>
    - `src/position.py` (`_append`, TP/SL prices)
    - `tests/phase05/test_position_machine_core.py`
  </read_first>
  <acceptance_criteria>
    - **`test_audit_07`**: at least one **`FillEvent`** for **TP1** has **`price == setup.target_price`** (exact) and **SL** at **`stop_price`**.
    - `grep -n "AUDIT-07" tests/phase07/test_audit_regression.py` matches.
  </acceptance_criteria>
  <action>
    Reuse **`simple_setup`** from **`tests/phase05/conftest.py`** (duplicate fixture in **`phase07/conftest`** if needed to avoid **`pytest_plugins`** issues).
  </action>
  <verify>
    <automated>python -m pytest tests/phase07/test_audit_regression.py -k audit_07 -q --tb=short</automated>
  </verify>
  <done>AUDIT-07 test green.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase07/test_audit_regression.py -q`
</verification>

<success_criteria>
- AUDIT-02..07 covered by automated tests with audit IDs in file.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trade-logic-audit-verification/07-00-SUMMARY.md`
</output>
