# Phase 07 — Verification

```bash
python -m pytest tests/phase07/ -q
python -m pytest tests/ -q
python -m src.audit --out audit_report.md
```

**AUDIT-01:** Manual two-run diff per `audit_report.md` MANUAL row.
