---
phase: 06-institutional-metrics
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics.py
  - tests/phase06/__init__.py
  - tests/phase06/conftest.py
  - tests/phase06/test_daily_equity_drawdown_pf.py
autonomous: true
requirements:
  - METRIC-01
  - METRIC-04
  - METRIC-05
  - METRIC-06
must_haves:
  truths:
    - "Daily net P&L summed by session_date; daily return = daily_pnl / account_size (fixed denominator per 06-CONTEXT D-01)"
    - "Compounded equity from daily returns; max drawdown vs rolling peak on equity (P8 / METRIC-04)"
    - "Profit factor uses C8 guard when no losing trades"
    - "Trade stats: count, win rate, avg win/loss, total net P&L (METRIC-06)"
  artifacts:
    - path: src/metrics.py
      provides: "Pure functions: daily_returns_table, compounded_equity_curve, max_drawdown_fraction, profit_factor_from_trades, basic_trade_stats"
  key_links:
    - from: "metrics.daily_returns_table"
      to: "TradeResult.session_date / net_pnl"
      via: "groupby ISO date string"
---

<objective>
Implement the **numerical core** for Phase 6: **daily P&L aggregation (METRIC-01)**, **compounded equity + max drawdown on rolling peak (METRIC-04, P8)**, **profit factor with zero-loser guard (METRIC-05, C8)**, and **basic trade statistics (METRIC-06)**.

Purpose: Deterministic, testable building blocks before Sharpe/Sortino and narrative reporting.

Output: Real `src/metrics.py` replacing the stub; `tests/phase06/` with golden-value tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-institutional-metrics/06-CONTEXT.md (D-01, D-03, D-04, D-09, D-12)
@.planning/phases/06-institutional-metrics/06-RESEARCH.md
@.planning/REQUIREMENTS.md (METRIC-01, METRIC-04, METRIC-05, METRIC-06)
@.planning/research/PITFALLS.md (P8, C8)
@src/position.py (`TradeResult`, `ExitType`)
@src/config.py (`StrategyThresholds.account_size`)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Daily P&L + return series (METRIC-01, D-01, D-03)</name>
  <files>src/metrics.py, tests/phase06/test_daily_equity_drawdown_pf.py, tests/phase06/conftest.py</files>
  <read_first>
    - `src/position.py` (`TradeResult` fields: `session_date`, `net_pnl`)
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-01, D-03)
    - `src/metrics.py` (current stub)
  </read_first>
  <acceptance_criteria>
    - `src/metrics.py` defines **`build_daily_pnl_and_returns(trades, account_size: float, zero_pnl_dates: list[str] | None = None) -> pd.DataFrame`** (or tuple of Series/DataFrame) with columns at minimum **`date`**, **`daily_net_pnl`**, **`daily_return`** where **`daily_return = daily_net_pnl / account_size`**.
    - For trades on the same `session_date`, net P&L sums: e.g. +800 and -200 on `"2024-06-03"` → **`daily_net_pnl == 600.0`** and **`daily_return == 600.0 / 100_000`** when `account_size=100_000`.
    - If **`zero_pnl_dates`** is provided and non-empty, each listed ISO date appears in the output with **`daily_net_pnl == 0`** and **`daily_return == 0.0`** even with no trades.
    - `grep -n "def build_daily" src/metrics.py` returns at least one match.
  </acceptance_criteria>
  <action>
    Replace stub **`metrics.py`**. Implement grouping by **`TradeResult.session_date`** (string sortable ISO `YYYY-MM-DD`). Use **`account_size`** parameter defaulting from **`StrategyThresholds.account_size`** only at call sites in later plans; function signature must take explicit **`account_size: float`**.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_daily_equity_drawdown_pf.py -k daily -q --tb=short</automated>
  </verify>
  <done>Daily aggregation tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compounded equity + max drawdown (METRIC-04, D-04, D-09, P8)</name>
  <files>src/metrics.py, tests/phase06/test_daily_equity_drawdown_pf.py</files>
  <read_first>
    - `.planning/research/PITFALLS.md` (P8)
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-04, D-09)
  </read_first>
  <acceptance_criteria>
    - **`compounded_equity_from_returns(daily_returns: pd.Series | Sequence[float], start_equity: float = 100_000) -> pd.Series`**: \(E_0 = 100000\), \(E_i = E_{i-1} \times (1 + r_i)\).
    - **`max_drawdown_stats(equity: pd.Series) -> dict`** returns keys **`max_drawdown_frac`** (negative or zero), **`max_drawdown_dollars`**, **`peak_idx`**, **`trough_idx`** where fraction uses **rolling peak**: `peak = equity.cummax()`, `dd = (equity - peak) / peak`, **`max_drawdown_frac = dd.min()`**.
    - Unit test constructs equity that **recovers then dips deeper**; the **second dip** yields the **more negative** `max_drawdown_frac` than the first (document numeric values in test).
    - `grep -n "cummax" src/metrics.py` matches at least once.
  </acceptance_criteria>
  <action>
    Implement on **ordered** daily returns aligned with the daily table row order. Do **not** compute drawdown as `(equity - start_equity) / start_equity`.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_daily_equity_drawdown_pf.py -k drawdown -q --tb=short</automated>
  </verify>
  <done>Drawdown ladder test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Profit factor + basic trade stats (METRIC-05, METRIC-06, C8)</name>
  <files>src/metrics.py, tests/phase06/test_daily_equity_drawdown_pf.py</files>
  <read_first>
    - `.planning/research/PITFALLS.md` (C8)
    - `.planning/REQUIREMENTS.md` (METRIC-05, METRIC-06)
  </read_first>
  <acceptance_criteria>
    - **`profit_factor(trades: list[TradeResult]) -> float`**: `gross_wins = sum(net_pnl for t in trades if t.net_pnl > 0)`, `gross_losses = sum(net_pnl for t in trades if t.net_pnl < 0)`. If **`gross_losses == 0`** and there is at least one winner, return **`gross_wins`** (finite). If no trades or all zeros, return **`0.0`** (document in docstring).
    - **`basic_trade_stats(trades) -> dict`** includes **`total_trades`**, **`win_rate`**, **`avg_win`**, **`avg_loss`**, **`total_net_pnl`** (definitions: win if `net_pnl > 0`, loss if `< 0`, exclude zeros from avg_win/avg_loss denominators or define explicitly in docstring and test).
    - Test: all trades profitable → **`profit_factor == sum(net_pnl)`** and **no `inf`**.
  </acceptance_criteria>
  <action>
    Use **`TradeResult.net_pnl`** only for P&L-based stats in this plan.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_daily_equity_drawdown_pf.py -k "profit or stats" -q --tb=short</automated>
  </verify>
  <done>PF and stats tests green.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase06/test_daily_equity_drawdown_pf.py -q`
</verification>

<success_criteria>
- METRIC-01, METRIC-04, METRIC-05, METRIC-06 numerics implemented with pytest coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-institutional-metrics/06-00-SUMMARY.md`
</output>
