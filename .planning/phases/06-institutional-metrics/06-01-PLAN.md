---
phase: 06-institutional-metrics
plan: 01
type: execute
wave: 2
depends_on:
  - 06-00
files_modified:
  - src/metrics.py
  - tests/phase06/test_sharpe_sortino_calmar.py
autonomous: true
requirements:
  - METRIC-02
  - METRIC-03
must_haves:
  truths:
    - "Sharpe = (mean daily return / std daily return) * sqrt(252) over full series including zeros (06-CONTEXT D-06)"
    - "Sortino per D-07 with annualized downside deviation; empty downside → documented sentinel"
    - "Calmar from compounded total return and max drawdown fraction (06-CONTEXT D-08)"
  artifacts:
    - path: src/metrics.py
      provides: "sharpe_ratio_annualized, sortino_ratio_annualized, calmar_ratio, recovery_factor, annual_breakdown_table"
  key_links:
    - from: "sharpe_ratio_annualized"
      to: "build_daily_pnl_and_returns daily_return column"
      via: "pandas Series of returns in chronological order"
---

<objective>
Implement **METRIC-02** (Sharpe) and **METRIC-03** (Sortino) per **`06-CONTEXT.md`**, plus **Calmar**, **recovery factor**, and **per-year breakdown** table inputs for Section 5 of `summary.md`.

Purpose: Risk-adjusted metrics on the same daily return series as Wave 1 (zero days included).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-institutional-metrics/06-CONTEXT.md (D-05, D-06, D-07, D-08)
@.planning/phases/06-institutional-metrics/06-RESEARCH.md
@.planning/REQUIREMENTS.md (METRIC-02, METRIC-03)
@src/metrics.py (functions from 06-00)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sharpe annualized (METRIC-02, D-05, D-06)</name>
  <files>src/metrics.py, tests/phase06/test_sharpe_sortino_calmar.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-05, D-06)
    - `.planning/REQUIREMENTS.md` (METRIC-02)
  </read_first>
  <acceptance_criteria>
    - **`sharpe_ratio_annualized(daily_returns: pd.Series, trading_days_per_year: int = 252) -> float | None`**: returns **`None`** (or `float('nan')` documented) if **`std == 0`** or **`len < 2`**.
    - Formula: **`(daily_returns.mean() / daily_returns.std(ddof=1)) * sqrt(trading_days_per_year)`** using **sample** std (`ddof=1`) unless docstring states population std; tests must lock the choice.
    - Golden test: fixed 5-day series with known mean/std → Sharpe matches hand calculation × sqrt(252).
    - `grep -n "sharpe_ratio_annualized" src/metrics.py` matches.
  </acceptance_criteria>
  <action>
    Risk-free rate **0**; do not subtract a constant from returns.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_sharpe_sortino_calmar.py -k sharpe -q --tb=short</automated>
  </verify>
  <done>Sharpe tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sortino annualized (METRIC-03, D-07)</name>
  <files>src/metrics.py, tests/phase06/test_sharpe_sortino_calmar.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-07)
  </read_first>
  <acceptance_criteria>
    - **`sortino_ratio_annualized(daily_returns: pd.Series, trading_days_per_year: int = 252) -> float | None`**: downside returns **`r < 0`** only; **`downside_variance = mean(r**2)`** for those **r**; **`downside_dev_daily = sqrt(downside_variance)`**; annualized denominator **`downside_dev_daily * sqrt(trading_days_per_year)`**; numerator **`mean(daily_returns) * trading_days_per_year`** per D-07.
    - If **no negative returns**: return **`None`** and expose **`sortino_downside_empty: bool`** via a **tuple return** or **separate probe function** documented in docstring; summary layer will print `inf` / flag per CONTEXT.
    - `grep -n "sortino" src/metrics.py` matches at least twice (def + logic).
  </acceptance_criteria>
  <action>
    Match **06-CONTEXT D-07** exactly; add comment citing D-07 if REQUIREMENTS METRIC-03 wording differs.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_sharpe_sortino_calmar.py -k sortino -q --tb=short</automated>
  </verify>
  <done>Sortino tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Calmar, recovery factor, annual breakdown</name>
  <files>src/metrics.py, tests/phase06/test_sharpe_sortino_calmar.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-08, D-11 Section 5)
    - `src/metrics.py` (`compounded_equity_from_returns`, `max_drawdown_stats`)
  </read_first>
  <acceptance_criteria>
    - **`calmar_ratio(total_return_frac: float, num_years: float, max_drawdown_frac: float) -> float | None`**: **`annualized_return = (1 + total_return_frac) ** (1/num_years) - 1`** when **`num_years > 0`**; **`calmar = annualized_return / abs(max_drawdown_frac)`** when **`max_drawdown_frac != 0`**.
    - **`recovery_factor(total_net_pnl: float, max_drawdown_dollars: float) -> float`**: **`total_net_pnl / abs(max_drawdown_dollars)`** when denominator non-zero; else **`0.0`**.
    - **`annual_breakdown(daily_df: pd.DataFrame) -> pd.DataFrame`**: group by **year** from date column; columns at minimum **`year`**, **`net_pnl`**, **`trades` count optional in later plan** — for this task include **`year`**, **`sum_daily_return`** or **`net_pnl`** aggregated from daily rows, and **`sharpe`** per year using **that year’s daily returns only** with **`sqrt(252)`**.
    - `grep -n "def calmar" src/metrics.py` matches.
  </acceptance_criteria>
  <action>
    **`num_years`** = elapsed calendar years from first to last date **inclusive**, minimum **`1/252`** if single day to avoid div-by-zero (document).
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_sharpe_sortino_calmar.py -k "calmar or annual or recovery" -q --tb=short</automated>
  </verify>
  <done>Calmar / recovery / annual tests pass.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase06/test_sharpe_sortino_calmar.py -q`
</verification>

<success_criteria>
- METRIC-02 and METRIC-03 satisfied with locked formulas in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-institutional-metrics/06-01-SUMMARY.md`
</output>
