---
phase: 06-institutional-metrics
plan: 02
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/summary_report.py
  - src/metrics.py
  - tests/phase06/test_summary_markdown_json.py
autonomous: true
requirements:
  - METRIC-01
  - METRIC-02
  - METRIC-03
  - METRIC-04
  - METRIC-05
  - METRIC-06
must_haves:
  truths:
    - "summary.md implements 9 sections per 06-CONTEXT D-11 (headings + tables + Section 8 YAML code block)"
    - "summary.json mirrors key numeric blocks for ML / dashboards"
    - "Known market event caption uses hardcoded date-range map (agent discretion table from CONTEXT)"
  artifacts:
    - path: src/summary_report.py
      provides: "build_summary_markdown(...), build_summary_dict(...), KNOWN_MARKET_EVENTS"
    - path: src/metrics.py
      provides: "drawdown_episodes (>=5% depth), calendar-day duration helpers if not already present"
  key_links:
    - from: "build_summary_markdown"
      to: "RunPaths.run_root / summary.md"
      via: "Path.write_text encoding utf-8"
---

<objective>
Produce the **institutional narrative** (**REPORT-04** superset): **9-section `summary.md`**, machine-readable **`summary.json`**, drawdown episode table (**Section 6**), and **parameter snapshot (Section 8)** from `config.yaml`.

Purpose: Human and machine consumers get one run artifact matching **`06-CONTEXT.md` D-11**.

Output: `src/summary_report.py` + tests asserting headings and JSON keys—not pixel-perfect prose.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-institutional-metrics/06-CONTEXT.md (D-10, D-11, D-12, Section 7–9)
@.planning/PROJECT.md (output tree)
@src/config.py (`load_config`, `AppConfig`)
@src/metrics.py (all computed series/stats from 06-00/06-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drawdown episodes + calendar duration (D-10, Section 6)</name>
  <files>src/metrics.py, tests/phase06/test_summary_markdown_json.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-10, Section 6)
    - `src/metrics.py` (`compounded_equity_from_returns`, `max_drawdown_stats`)
  </read_first>
  <acceptance_criteria>
    - Function **`list_drawdown_episodes(dates, equity, min_depth_frac: float = 0.05, max_rows: int = 10) -> list[dict]`** returns episodes with **`peak_date`**, **`trough_date`**, **`recovery_date`** (first date equity **>** prior peak after trough, or **`None`** if unrecovered), **`depth_frac`**, **`depth_dollars`**, **`duration_calendar_days`** = **`(recovery_or_last - peak).days`** using **`datetime.date`** parsing from ISO strings.
    - Filter episodes where **`abs(depth_frac) >= min_depth_frac`** (depth negative fraction), sort by depth ascending (most negative first), cap **`max_rows`**.
    - Test uses 4–6 synthetic daily points with known peak/trough/recovery.
    - `grep -n "list_drawdown_episodes" src/metrics.py` matches.
  </acceptance_criteria>
  <action>
    **`dates`** aligned 1:1 with **`equity`** Series values (same length).
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_summary_markdown_json.py -k episodes -q --tb=short</automated>
  </verify>
  <done>Episode list test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_summary_dict (JSON)</name>
  <files>src/summary_report.py, tests/phase06/test_summary_markdown_json.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (summary.json mention)
    - `src/metrics.py` (exported stats functions)
  </read_first>
  <acceptance_criteria>
    - **`build_summary_dict(trades, daily_df, equity_stats, risk_stats, session_diag: dict, run_meta: dict) -> dict`** returns JSON-serializable dict with top-level keys at minimum: **`meta`**, **`risk_adjusted`**, **`trade_stats`**, **`daily_distribution`**, **`annual`**, **`drawdown_episodes`**, **`session_quality`**, **`warnings`**.
    - **`json.dumps(build_summary_dict(...), default=str)`** succeeds in test.
    - `src/summary_report.py` contains **`KNOWN_MARKET_EVENTS: list[tuple[tuple[str,str], str]]`** or dict structure with **≥ 2** entries (e.g. COVID, 2022 hikes) per CONTEXT discretion.
    - `grep -n "KNOWN_MARKET" src/summary_report.py` matches.
  </acceptance_criteria>
  <action>
    **`run_meta`** must include **`run_timestamp`**, **`instrument`**, **`config_path`** strings for Section 1/8 provenance.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_summary_markdown_json.py -k json -q --tb=short</automated>
  </verify>
  <done>JSON round-trip test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: build_summary_markdown (9 sections)</name>
  <files>src/summary_report.py, tests/phase06/test_summary_markdown_json.py</files>
  <read_first>
    - `.planning/phases/06-institutional-metrics/06-CONTEXT.md` (D-11 full outline)
    - `.planning/PROJECT.md` (paths note)
  </read_first>
  <acceptance_criteria>
    - **`build_summary_markdown(payload: dict | SummaryPayload dataclass) -> str`** output contains **Markdown H2 headings** (or H1 for title + H2 sections) with **exact substring** matches in order: **`Executive Summary`**, **`Risk-Adjusted Performance`**, **`Trade Statistics`**, **`Daily P&L Distribution`**, **`Annual Breakdown`**, **`Drawdown Analysis`**, **`Session & Setup Quality`**, **`Parameter Snapshot`**, **`Notes & Warnings`**.
    - **Section 8** contains a **fenced code block** with **`yaml`** or triple-backtick and **verbatim** text passed in from **`config_yaml_text`** parameter (test passes minimal 2-line YAML string).
    - **Section 6** table includes at least one row when synthetic episodes list non-empty.
    - `grep -n "def build_summary_markdown" src/summary_report.py` matches.
  </acceptance_criteria>
  <action>
    Trade statistics must include **setup_type** breakdown (**THREE_STEP** vs **AGGRESSIVE_LEDGE**) and **signal_source** breakdown (**ISMT** vs **SMT**) using **`TradeResult.setup_type`** and **`TradeResult.signal_source`** (`1` = ISMT, `0` = **SMT** per `entry.signal_source_value`). **Scratch** trades: **`ExitType.PARTIAL_TP`** and **`abs(net_pnl) < 1e-6`** (document epsilon).
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_summary_markdown_json.py -k markdown -q --tb=short</automated>
  </verify>
  <done>Heading/substring tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Write files API</name>
  <files>src/summary_report.py, tests/phase06/test_summary_markdown_json.py</files>
  <read_first>
    - `src/config.py` (`RunPaths`)
  </read_first>
  <acceptance_criteria>
    - **`write_run_summaries(run_root: Path, md_text: str, json_obj: dict) -> tuple[Path, Path]`** writes **`run_root / "summary.md"`** and **`run_root / "summary.json"`** with **`encoding="utf-8"`**; creates **`run_root`** if needed.
    - Test uses **`tmp_path`** and asserts both files exist and **`summary.json`** parses.
  </acceptance_criteria>
  <action>
    Use **`json.dump(..., indent=2)`** for JSON.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_summary_markdown_json.py -k write_run -q --tb=short</automated>
  </verify>
  <done>Write API test passes.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase06/test_summary_markdown_json.py -q`
</verification>

<success_criteria>
- METRIC reporting surfaces complete; D-11 structure verified by automated substring checks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-institutional-metrics/06-02-SUMMARY.md`
</output>
