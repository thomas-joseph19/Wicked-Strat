---
phase: 06-institutional-metrics
plan: 03
type: execute
wave: 4
depends_on:
  - 06-02
files_modified:
  - src/backtest.py
  - src/reporting.py
  - tests/phase06/test_metrics_backtest_wiring.py
  - tests/phase05/test_summary_stub.py
autonomous: true
requirements:
  - METRIC-01
  - METRIC-02
  - METRIC-03
  - METRIC-04
  - METRIC-05
  - METRIC-06
must_haves:
  truths:
    - "Optional end-of-run hook computes metrics + writes summary.md/json without breaking Phase 5 CSV/HTML flow"
    - "Phase 5 summary stub test updated or parameterized so CI stays green"
  artifacts:
    - path: src/backtest.py
      provides: "Keyword flag write_institutional_summaries + call into metrics/summary_report"
    - path: src/reporting.py
      provides: "Optional helper or documented call order with RunWriter.rows buffer"
  key_links:
    - from: "run_session_backtest"
      to: "write_run_summaries"
      via: "accumulated TradeResult list + paths.run_root"
---

<objective>
**Wire** the metrics stack into the **Phase 5 backtest path**: after trades are collected and CSV/HTML written, optionally **compute institutional metrics** and **overwrite / augment** run-level **`summary.md`** + **`summary.json`**.

Purpose: One command produces per-trade artifacts **and** final institutional summary (ROADMAP Phase 6 goal).

Output: Integration test on **`tmp_path`**; Phase 5 tests remain passing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/backtest.py
@src/reporting.py (`RunWriter.rows`)
@src/config.py (`RunPaths`, `load_config`)
@src/summary_report.py
@src/metrics.py
@tests/phase05/test_summary_stub.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_run_institutional_metrics orchestration function</name>
  <files>src/metrics.py or src/run_metrics.py, tests/phase06/test_metrics_backtest_wiring.py</files>
  <read_first>
    - `src/metrics.py` (all public stats + daily table builders)
    - `src/summary_report.py` (`build_summary_dict`, `build_summary_markdown`, `write_run_summaries`)
    - `src/config.py` (`StrategyThresholds`)
  </read_first>
  <acceptance_criteria>
    - A single entrypoint **`compute_and_write_institutional_summaries(trades: list[TradeResult], paths: RunPaths, account_size: float, config_yaml_text: str, instrument_symbol: str, run_timestamp: str, zero_pnl_dates: list[str] | None = None, session_diag: dict | None = None) -> None`** exists in **`src/metrics.py`** OR new **`src/run_metrics.py`** (if new file, **`grep -n "compute_and_write" src/run_metrics.py`** matches).
    - Invoking it with **≥ 1** synthetic **`TradeResult`** creates **`paths.run_root / "summary.md"`** and **`summary.json`** with **file size > 100 bytes** each.
    - `session_diag` default **`None`** → summary uses **empty Section 7 placeholders** documented in **`build_summary_markdown`** (zeros or “N/A”).
  </acceptance_criteria>
  <action>
    Compose **`daily_df`**, **`equity`**, risk metrics, **`build_summary_dict`**, **`build_summary_markdown`**, then **`write_run_summaries`**. Pass **`RunWriter.rows`** trade count if available by accepting optional **`writer: RunWriter | None`** and reading **`len(writer.rows)`** for meta—**or** use **`len(trades)`** only (document).
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_metrics_backtest_wiring.py -k compute_and_write -q --tb=short</automated>
  </verify>
  <done>Orchestration test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backtest.py integration flag</name>
  <files>src/backtest.py, tests/phase06/test_metrics_backtest_wiring.py</files>
  <read_first>
    - `src/backtest.py` (full file)
  </read_first>
  <acceptance_criteria>
    - **`run_session_backtest`** adds keyword-only argument **`write_institutional_summaries: bool = False`** (default **False** preserves current behavior).
    - When **`True`**, after the setup loop, if **`results`** non-empty, call **`compute_and_write_institutional_summaries(...)`** with **`paths`**, **`account_size=thresholds.account_size`**, **`run_timestamp=paths.run_timestamp`**, **`instrument_symbol=instrument.symbol`**.
    - **`config_yaml_text`**: read from a parameter **`config_path: str = "config.yaml"`** when flag True; if file missing, write placeholder string **`"# config.yaml not found\n"`** in Section 8 block (test with missing file in **`tmp_path`** by passing nonexistent path).
    - `grep -n "write_institutional_summaries" src/backtest.py` matches.
  </acceptance_criteria>
  <action>
    Do **not** remove per-trade CSV/HTML logic.
  </action>
  <verify>
    <automated>python -m pytest tests/phase06/test_metrics_backtest_wiring.py -k backtest_flag -q --tb=short</automated>
  </verify>
  <done>Flag test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 5 summary stub test compatibility</name>
  <files>tests/phase05/test_summary_stub.py</files>
  <read_first>
    - `tests/phase05/test_summary_stub.py`
    - `src/reporting.py` (`RunWriter.finalize_summary`)
  </read_first>
  <acceptance_criteria>
    - **`test_finalize_summary_net_total`** still passes **`python -m pytest tests/phase05/test_summary_stub.py -q`**.
    - If assertion text **`Phase 5 stub`** is too strict once institutional writer exists, broaden to **substring** **`summary.md`** exists **and** (**`Phase 5 stub`** **OR** **`Phase 6`** **OR** **`institutional`**) — document in comment.
  </acceptance_criteria>
  <action>
    Prefer **minimal** change: only relax assertion if Phase 6 reuses **`finalize_summary`** (not default). If **`finalize_summary`** unchanged, **no edit** to Phase 5 test required.
  </action>
  <verify>
    <automated>python -m pytest tests/phase05/test_summary_stub.py -q</automated>
  </verify>
  <done>Phase 5 test green.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase06/ tests/phase05/test_summary_stub.py -q`
</verification>

<success_criteria>
- End-to-end wiring behind default-off flag; METRIC-01..06 reachable from one orchestration call.
</success_criteria>

<output>
After completion, create `.planning/phases/06-institutional-metrics/06-03-SUMMARY.md`
</output>
