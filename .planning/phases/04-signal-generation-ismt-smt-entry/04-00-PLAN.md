---
phase: 04-signal-generation-ismt-smt-entry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/phase04/__init__.py
  - tests/phase04/conftest.py
  - src/position.py
autonomous: true
requirements:
  - ISMT-01
  - ISMT-02
  - ISMT-03
  - SMT-01
  - SMT-02
  - SMT-03
  - SMT-04
  - SMT-05
  - SIG-01
  - SIG-02
  - SIG-03
  - ENTRY-01
  - ENTRY-02
  - ENTRY-03
  - ENTRY-04
  - ENTRY-05
must_haves:
  truths:
    - "pytest discovers tests under tests/phase04/"
    - "TradeSetup dataclass exists with EXEC-01 fields plus signal_source for SIG-03"
  artifacts:
    - path: pyproject.toml
      provides: "[tool.pytest.ini_options] with testpaths including tests"
    - path: tests/phase04/conftest.py
      provides: "fixtures for bars, swings, LVN, SP, synced stream"
    - path: src/position.py
      provides: "TradeSetup definition (no longer empty stub)"
  key_links:
    - from: "tests/phase04/"
      to: "src/"
      via: "imports for ismt/smt/entry tests"
---

<objective>
Establish **shared automated verification infrastructure** for Phase 4 (pytest layout, fixtures) and the **`TradeSetup` contract** Phase 5 will consume. This plan does **not** implement detectors; it unblocks parallel implementation in `04-01` and `04-02`.

Purpose: Prevent merge conflicts on `conftest.py` and guarantee one canonical `TradeSetup` shape.

Output: `pyproject.toml` pytest section, `tests/phase04/` package, `src/position.py` with `TradeSetup`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-signal-generation-ismt-smt-entry/04-RESEARCH.md
@.planning/research/PITFALLS.md (P3 intra-bar, P7 synthetic SMT)

<interfaces>
From `src/lvn.py` (executor must match field names used by entry tests):

```python
@dataclass
class LVNZone:
    low: float
    high: float
    midpoint: float
    ...
    valid: bool = True
```

From `src/swings.py`:

```python
@dataclass(frozen=True)
class SwingPoint:
    bar_index: int
    price: float
    direction: str  # 'HIGH' or 'LOW'
    confirmed_at: int
```

From `src/session.py`:

```python
class WindowConfig:
    EOD_CUTOFF = time(15, 45)
    AGGRESSIVE_START = time(9, 25)
    AGGRESSIVE_END = time(10, 0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pytest layout and pyproject.toml</name>
  <read_first>
    - `.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md` (canonical_refs)
    - `.planning/phases/04-signal-generation-ismt-smt-entry/04-VALIDATION.md` (Wave 0 checklist)
    - `pyproject.toml` (create at repo root if missing)
  </read_first>
  <acceptance_criteria>
    - `grep -n "testpaths" pyproject.toml` matches `tests` (and allows `tests/phase04` discovery).
    - `tests/phase04/__init__.py` exists (may be empty).
    - `python -m pytest tests/phase04/ --collect-only -q` exits 0 (zero tests OK on first commit).
  </acceptance_criteria>
  <action>
    Create or extend **repo-root** `pyproject.toml` with `[tool.pytest.ini_options]`:
    `testpaths = ["tests"]`, `pythonpath = ["."]` (or equivalent so `from src...` imports work on Windows).
    Add `tests/phase04/__init__.py`.
    Do **not** add placeholder test files yet (plans 04-01+ own test file creation).
  </action>
  <verify>
    <automated>python -m pytest tests/phase04/ --collect-only -q</automated>
  </verify>
  <done>Pytest collects an empty `tests/phase04/` package without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: conftest fixtures + TradeSetup in position.py</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — EXEC-01 (TradeSetup fields), SIG-03 (`signal_source` encoding)
    - `src/position.py` (replace stub)
    - `src/lvn.py`, `src/swings.py` (types for type hints)
  </read_first>
  <acceptance_criteria>
    - `grep -n "class TradeSetup" src/position.py` exists.
    - `grep -n "signal_source" src/position.py` exists (int: ISMT=1, SMT=0 per D-07 / SIG-03).
    - `grep -n "def make_bar" tests/phase04/conftest.py` OR equivalent factory helpers exist.
    - `python -c "from src.position import TradeSetup; import dataclasses; print(dataclasses.fields(TradeSetup))"` exits 0.
  </acceptance_criteria>
  <action>
    **`src/position.py`:** Define `@dataclass` `TradeSetup` with at minimum EXEC-01 fields:
    `setup_id: str`, `entry_price: float`, `stop_price: float`, `target_price: float` (TP1),
    `rr_ratio: float`, `direction: str` (use `LONG`/`SHORT` consistently with existing `entry.py` or migrate to `BULLISH`/`BEARISH` **once** in plan 04-04 — this task must pick one convention and document in module docstring),
    `created_at` (bar index `int` **or** `pd.Timestamp` — pick **int bar index** for backtest determinism unless PROJECT.md mandates timestamp),
    `setup_type` (`Literal["THREE_STEP","AGGRESSIVE_LEDGE"]` or str enum),
    `lvn_ref` (reference to `LVNZone` or stable string id — prefer **object reference + optional `lvn_id: str`** if needed for sets),
    `ismt_or_smt_ref` (`Any | None` — forward reference to Ismt/Smt signal types defined in later plans),
    **`signal_source: int`** per SIG-03 / D-07 (ISMT=1, SMT=0, Aggressive ledge with no structural signal = document as `None` or separate sentinel; if aggressive has no ISMT/SMT, use `signal_source=0` and `ismt_or_smt_ref=None` only if tests in 04-04 require — default `Optional[int] = None` allowed if documented).

    **`tests/phase04/conftest.py`:** Add minimal factories:
    - `tick_size = 0.25`
    - `make_swing(bar_index, confirmed_at, price, direction)` → `SwingPoint`
    - `make_lvn(low, high, valid=True)` → `LVNZone` with `midpoint=(low+high)/2`, `width_ticks=int(round((high-low)/tick_size))`, `strength=0.5`, `session_id="test"`
    - `make_sp_zone(low, high, respected_overnight: bool)` simple namespace or dataclass for tests
    - `make_sync_row(i, o,h,l,c, syn_nq=False, syn_es=False)` building one row of a stream with columns `open_nq, high_nq, low_nq, close_nq, open_es, high_es, low_es, close_es, is_synthetic_nq, is_synthetic_es` **or** the exact column names used in `src/data_loader.py` / `src/smt.py` today — **read `src/data_loader.py` and match actual column names** so SMT tests do not fight the codebase.

    Remove/replace `stub_func` in `position.py`; keep other Phase 5 stubs as `NotImplementedError` if present.
  </action>
  <verify>
    <automated>python -m pytest tests/phase04/ --collect-only -q</automated>
  </verify>
  <done>TradeSetup importable; conftest imports without error when loaded by pytest.</done>
</task>

</tasks>

<verification>
Wave 0 gate: `python -m pytest tests/phase04/ --collect-only -q` and TradeSetup import smoke pass.
</verification>

<success_criteria>
- No duplicate conftest creation in later parallel plans.
- Downstream plans can `from src.position import TradeSetup` immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/04-signal-generation-ismt-smt-entry/04-00-SUMMARY.md`
</output>
