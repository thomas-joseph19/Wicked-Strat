---
phase: 04-signal-generation-ismt-smt-entry
plan: 01
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - src/ismt.py
  - tests/phase04/test_ismt.py
autonomous: true
requirements:
  - ISMT-01
  - ISMT-02
  - ISMT-03
must_haves:
  truths:
    - "Bearish ISMT fires only when a bar in (SH2.confirmed_at+1 .. SH2.confirmed_at+3) closes strictly below SH1.price"
    - "Bullish ISMT mirrors with swing lows and close strictly above SL1.price"
    - "sweep_size < 2 × ATR20 at the signal completion bar (ISMT-01/02)"
    - "1 <= SH2.confirmed_at - SH1.confirmed_at <= 10 (confirmation-clock only)"
  artifacts:
    - path: src/ismt.py
      provides: "IsmtSignal + detection API consumed by entry / structural confirmation"
    - path: tests/phase04/test_ismt.py
      provides: "grep-able tests test_ismt_*"
  key_links:
    - from: "detect_ismt_at_bar"
      to: "SwingPoint.confirmed_at"
      via: "close-back window indices t+1..t+3"
---

<objective>
Implement **hindsight-free ISMT** on NQ using **only** swings with `confirmed_at <= current_bar_index`, anchored to **D-01/D-02** (close-back window starts `SH2.confirmed_at + 1`). Emit **`IsmtSignal`** payloads per **ISMT-03**.

Purpose: ISMT is the single-instrument structural primitive; bugs here poison all entries.

Output: `src/ismt.py` + `tests/phase04/test_ismt.py` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md (D-01, D-02, D-03)
@.planning/REQUIREMENTS.md (ISMT-01..03)
@.planning/research/PITFALLS.md (P3 bar-close evaluation)

<interfaces>
Existing `src/ismt.py` uses `ISMTConfirmation` and `SwingPoint`; **replace or alias** with a unified `IsmtSignal` dataclass:

Required fields (**ISMT-03**): `confirmed_at: int`, `sweep_size: float`, direction, `entry_zone: float` (use **SH1.price** bearish, **SL1.price** bullish as reference), plus `signal_kind: Literal["ISMT"] = "ISMT"`, `source_swings: tuple[SwingPoint, ...]`, `invalidated: bool = False`.

**D-01/D-02 (hard numbers):** Let `t = SH2.confirmed_at` (bearish) or `t = SL2.confirmed_at` (bullish). Eligible completion bars `j` satisfy `t + 1 <= j <= t + 3` **and** `j == current_bar_index` when detector runs (bar-close).

**ISMT-01 timing:** Require `SH2.price > SH1.price` (bearish). Require `1 <= (SH2.confirmed_at - SH1.confirmed_at) <= 10`.

**Sweep / ATR:** `sweep_size = SH2.price - SH1.price` (bearish) or `SL1.price - SL2.price` (bullish, positive magnitude). At candidate completion bar `j`, require `sweep_size < 2.0 * atr20[j]` where `atr20[j]` is the **same series Phase 1/core uses** (executor: read `src/core.py` for function name and pass precomputed array).

**D-03:** Detector may emit signals that later fail `get_structural_confirmation` recency — acceptable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IsmtSignal dataclass + pure detector API</name>
  <read_first>
    - `src/ismt.py` (current implementation to refactor)
    - `.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md` (D-01–D-03)
    - `src/swings.py` (`SwingPoint`)
    - `src/core.py` (ATR series accessor pattern)
  </read_first>
  <acceptance_criteria>
    - `grep -n "class IsmtSignal" src/ismt.py` exists.
    - `grep -n "signal_kind" src/ismt.py` includes `"ISMT"`.
    - Public function exists named either `detect_ismt_for_bar` or `detect_ismt_at_bar` documented in module docstring.
    - No read of `bars[current_bar_index+1]` in detector (static review: grep `+ 1]` patterns only in safe contexts).
  </acceptance_criteria>
  <action>
    Refactor `src/ismt.py`:
    - Remove incorrect window `sh2.confirmed_at < bar_idx <= sh2.confirmed_at + 3` if it allows bar `t` — **must exclude** `j <= t`; **only** `j in [t+1, t+2, t+3]`.
    - Bearish: last two confirmed **HIGH** swings in merged swing list (caller passes swings visible at `j` with `confirmed_at <= j`). Validate `SH2.price > SH1.price` and confirmation delta **1..10** bars on **`confirmed_at`** indices only.
    - Bullish: last two **LOW** swings, `SL2.price < SL1.price`, same windows with closes **>** `SL1.price`.
    - `confirmed_at` on **`IsmtSignal`** = completion bar index `j` where close-back first becomes true within the 3-bar window (if multiple bars qualify, **earliest** `j` in `t+1..t+3` wins to avoid duplicate fires).
    - `sweep_size` as defined in `<interfaces>`.
    - `entry_zone` = `SH1.price` (bearish) / `SL1.price` (bullish).
    - Optional: `invalidate_ismt_on_trade_through(signal, bars_up_to_j)` helper for D-06 (can be stub returning unchanged if 04-03 owns invalidation — prefer implement **bearish rule**: if any bar after signal has **close > SH1.price**, set `invalidated=True`).
  </action>
  <verify>
    <automated>python -c "from src.ismt import IsmtSignal; print(IsmtSignal.__name__)"</automated>
  </verify>
  <done>IsmtSignal and detector API import without syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/phase04/test_ismt.py — ISMT-01/02/03</name>
  <read_first>
    - `tests/phase04/conftest.py`
    - `.planning/REQUIREMENTS.md` (ISMT-01..03)
    - `src/ismt.py`
  </read_first>
  <behavior>
    - `test_ismt_bearish_fires_bar_t_plus_1_when_close_back`: SH1 at `t1`, SH2 confirmed `t`, first close below SH1 at `t+1` → signal `confirmed_at == t+1`.
    - `test_ismt_no_lookahead_window_uses_confirmed_at_not_bar_index`: if close-back happens at `SH2.bar_index+1` but **before** `SH2.confirmed_at+1`, **no** signal (proves D-01).
    - `test_ismt_sweep_rejected_at_2x_atr20_boundary`: fix ATR20 constant 1.0, sweep 2.0+epsilon → no signal; 1.99 → signal (adjust for float tolerance 1e-9).
    - `test_ismt_confirmation_spacing_rejects_gt_10`: `SH2.confirmed_at - SH1.confirmed_at == 11` → no signal.
    - `test_ismt_signal_fields_populated`: assert `IsmtSignal.sweep_size`, `entry_zone`, `signal_kind == "ISMT"` present (ISMT-03).
  </behavior>
  <acceptance_criteria>
    - `grep -n "def test_ismt_bearish_fires_bar_t_plus_1_when_close_back" tests/phase04/test_ismt.py` exists.
    - `grep -n "def test_ismt_no_lookahead_window_uses_confirmed_at_not_bar_index" tests/phase04/test_ismt.py` exists.
    - `grep -n "def test_ismt_sweep_rejected_at_2x_atr20_boundary" tests/phase04/test_ismt.py` exists.
    - `grep -n "def test_ismt_confirmation_spacing_rejects_gt_10" tests/phase04/test_ismt.py` exists.
    - `grep -n "def test_ismt_signal_fields_populated" tests/phase04/test_ismt.py` exists.
    - `python -m pytest tests/phase04/test_ismt.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <action>
    Add `tests/phase04/test_ismt.py`. Build **minimal OHLC arrays** (list of dicts or DataFrame rows) and synthetic `atr20` arrays; pass swings as explicit `SwingPoint` instances with hand-set `bar_index` and `confirmed_at`.

    Use **NQ close series** field name consistent with project (`close` or `close_nq` — match `ismt.py` signature you implement).

    Run RED→GREEN if needed; ensure tests encode **D-01/D-02** literally.
  </action>
  <verify>
    <automated>python -m pytest tests/phase04/test_ismt.py -q --tb=short</automated>
  </verify>
  <done>All listed behaviors green; pytest exit code 0.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase04/test_ismt.py -q`
</verification>

<success_criteria>
- ISMT windows and sweep filter match REQUIREMENTS + CONTEXT.
- At least one negative-control test proves `confirmed_at` anchoring (not `bar_index`).
</success_criteria>

<output>
After completion, create `.planning/phases/04-signal-generation-ismt-smt-entry/04-01-SUMMARY.md`
</output>
