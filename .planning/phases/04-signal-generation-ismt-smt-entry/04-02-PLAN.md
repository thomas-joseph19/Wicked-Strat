---
phase: 04-signal-generation-ismt-smt-entry
plan: 02
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - src/smt.py
  - tests/phase04/test_smt.py
autonomous: true
requirements:
  - SMT-01
  - SMT-02
  - SMT-03
  - SMT-04
  - SMT-05
must_haves:
  truths:
    - "Rolling Pearson correlation on 20 NQ+ES returns; SMT only if corr >= 0.70 (SMT-02)"
    - "If >2 bars in the 20-bar window have is_synthetic==True, skip correlation + SMT (Phase 1 D-06)"
    - "If either NQ or ES bar at the divergence evaluation index is synthetic, no SMT (P7 / SMT-03)"
    - "Bearish: NQ up-move >= 0.3×ATR20, ES up-move <= +0.1×ATR20, swing confirmations within ±3 bars"
    - "Bullish mirror: NQ down-move <= -0.3×ATR20, ES down-move >= -0.1×ATR20"
  artifacts:
    - path: src/smt.py
      provides: "SmtSignal + correlation + divergence detection"
    - path: tests/phase04/test_smt.py
      provides: "correlation gate + synthetic + divergence tests"
  key_links:
    - from: "rolling_corr"
      to: "numpy.corrcoef"
      via: "20 consecutive simple returns ending at bar i"
---

<objective>
Implement **dual-instrument SMT** with **Pearson correlation gating**, **synthetic bar suppression** (P7 + D-06), and **ATR-scaled divergence** per **SMT-03/04**. Emit **`SmtSignal`** with **SMT-05** fields.

Purpose: False SMT on illiquid/synthetic ES is a silent P&L killer (P7).

Output: `src/smt.py` + `tests/phase04/test_smt.py` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-data-infrastructure-core-candle-math/01-CONTEXT.md (D-06 synthetic correlation window)
@.planning/research/PITFALLS.md (P7)
@.planning/phases/04-signal-generation-ismt-smt-entry/04-RESEARCH.md (correlation return definition)

<interfaces>
**Returns for correlation (freeze in code + tests):** For bar index `k>=1`, `r_nq[k] = (close_nq[k]-close_nq[k-1])/close_nq[k-1]` (guard divide-by-zero); at evaluation bar `i`, take **20 returns** `r_nq[i-19], ..., r_nq[i]` **inclusive** (requires `i>=19` and **21** closes available). Same for ES.

**Synthetic window (D-06):** Count bars `k in [i-19, i]` where stream row has `is_synthetic == True` (column present in `data_loader` — **verify exact name** `is_synthetic` vs per-leg; use **the same boolean the project uses for “either leg synthetic”** — read `src/data_loader.py` line defining `is_synthetic`).

If `synthetic_count > 2`: return `corr = nan` and **do not** emit SMT.

**Per-bar synthetic (P7):** When comparing the **NQ** and **ES** swing confirmation events, if **either** instrument’s bar at the chosen evaluation index has `is_synthetic_nq` or `is_synthetic_es` True (match `data_loader` columns), **abort** SMT for that candidate.

**SmtSignal (SMT-05):** `confirmed_at: int`, `divergence_strength: float` (use `(nq_move - es_move) / atr20[i]` for bearish highs; for bullish lows use mirror formula `(es_move - nq_move) / atr20[i]` or document single signed definition), `correlation_at_signal: float`, `signal_kind: Literal["SMT"]="SMT"`, `invalidated: bool=False`, plus references to the two swings used (`nq_swing`, `es_swing`).

**±3 bars:** `abs(nq_high.confirmed_at - es_high.confirmed_at) <= 3` using the **new** swing confirmations forming the divergence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correlation + synthetic gates (SMT-01, SMT-02)</name>
  <read_first>
    - `src/smt.py`
    - `src/data_loader.py` (`close_nq`, `close_es`, `is_synthetic` / per-leg flags)
    - `.planning/phases/01-data-infrastructure-core-candle-math/01-CONTEXT.md` (D-06)
  </read_first>
  <acceptance_criteria>
    - Helper function exists, e.g. `pearson_corr_20(nq_closes, es_closes, i) -> float` OR `corr_at_bar(stream, i)`.
    - `grep -n "corrcoef" src/smt.py` uses `numpy.corrcoef`.
    - When `synthetic_count > 2` in `[i-19,i]`, function returns `nan` or a sentinel and **callers skip SMT**.
  </acceptance_criteria>
  <action>
    Replace ad-hoc `np.diff` window that yields **19** diffs from **20** closes — align to **20 returns** per `<interfaces>`.

    Implement `corr >= 0.70` check with **exclusive** rejection below threshold; `nan` → no SMT.

    Document column names used (`close_nq`, `close_es`, `is_synthetic` or per-leg) in module docstring.
  </action>
  <verify>
    <automated>python -c "import importlib; importlib.import_module('src.smt')"</automated>
  </verify>
  <done>Correlation helper implemented; ready for divergence wiring in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Divergence rules + SmtSignal + test_smt.py</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` (SMT-03, SMT-04, SMT-05)
    - `src/smt.py`
    - `tests/phase04/conftest.py`
  </read_first>
  <behavior>
    - `test_smt_correlation_065_suppresses_085_allows`: construct two 20-return vectors with Pearson ~0.65 and ~0.85 (use fixed arrays; assert threshold 0.70).
    - `test_smt_skips_when_more_than_two_synthetic_in_window`: 3+ `is_synthetic` True in `[i-19,i]` → no signal.
    - `test_smt_skips_when_es_synthetic_at_confirm`: ES synthetic at evaluation bar while NQ real → no signal.
    - `test_smt_bearish_thresholds_and_bar_alignment`: NQ HH move = 0.31×ATR20, ES move = 0.09×ATR20, `|confirm_nq - confirm_es| <= 3` → signal; move 0.29× / 0.11× → no signal.
    - `test_smt_bullish_mirror`: symmetric for lows.
    - `test_smt_signal_fields`: `correlation_at_signal`, `divergence_strength`, `confirmed_at` populated (SMT-05).
  </behavior>
  <action>
    Implement `detect_smt_at_bar(stream_df, i, nq_swings, es_swings, atr20, ...)` or class method:

    - Select **last two confirmed HIGH** swings for NQ and ES for bearish pattern (all `confirmed_at <= i`).
    - Compute moves vs prior swing in same direction.
    - Apply ATR scaling with **`atr20[i]`**.
    - `confirmed_at` for signal = **latest** confirmation bar among the two swings when the pattern validates (document in docstring).

    Refactor/remove logic that requires `nq_s.confirmed_at == current_idx` unless it matches spec (REQ allows ±3 — **do not** require same bar).

    Add `tests/phase04/test_smt.py` covering all `<behavior>` cases using **minimal DataFrames** with explicit `close_nq`, `close_es`, `is_synthetic` (+ per-leg if required by implementation).
  </action>
  <verify>
    <automated>python -m pytest tests/phase04/test_smt.py -q --tb=short</automated>
  </verify>
  <done>Full SMT-01..05 behaviors covered; pytest exit 0.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase04/test_smt.py -q`
</verification>

<success_criteria>
- Correlation and synthetic gates match Phase 1 D-06 + P7.
- Divergence numeric thresholds match REQUIREMENTS literals (0.70, 0.3, 0.1, ±3).
</success_criteria>

<output>
After completion, create `.planning/phases/04-signal-generation-ismt-smt-entry/04-02-SUMMARY.md`
</output>
