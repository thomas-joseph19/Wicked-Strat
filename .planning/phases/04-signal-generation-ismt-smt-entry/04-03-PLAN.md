---
phase: 04-signal-generation-ismt-smt-entry
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/entry.py
  - tests/phase04/test_structural_confirmation.py
autonomous: true
requirements:
  - SIG-01
  - SIG-02
  - SIG-03
must_haves:
  truths:
    - "get_structural_confirmation(direction, i, ...) considers only signals with confirmed_at in [i-4, i] inclusive (D-05 / 5 bars)"
    - "ISMT and SMT have equal priority; winner is max confirmed_at among non-invalidated candidates (D-04)"
    - "SIG-02 (REQ ISMT-first) is NOT implemented; tests assert SMT can beat ISMT when more recent"
    - "signal_source mapping available: ISMT→1, SMT→0 (SIG-03 / D-07)"
  artifacts:
    - path: src/entry.py
      provides: "get_structural_confirmation + helper signal_source_of"
    - path: tests/phase04/test_structural_confirmation.py
      provides: "D-04..D-06 regression tests"
  key_links:
    - from: "get_structural_confirmation"
      to: "IsmtSignal | SmtSignal lists"
      via: "filter by direction + window + invalidated"
---

<objective>
Implement **`get_structural_confirmation()`** as a **pure selector** over precomputed ISMT/SMT candidates per **D-04–D-06**, superseding REQ **SIG-01/SIG-02** ordering language. Expose **`signal_source`** encoding for **SIG-03**.

Purpose: Centralize structural choice so 3-Step entries cannot accidentally reintroduce “ISMT first.”

Output: Updated `src/entry.py` + `tests/phase04/test_structural_confirmation.py`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md (D-04, D-05, D-06, D-07)
@.planning/phases/04-signal-generation-ismt-smt-entry/04-RESEARCH.md (selector algorithm)

<interfaces>
**Signature (implement exactly this shape or extend with optional args, but keep these names):**

```python
def get_structural_confirmation(
    direction: Literal["BULLISH", "BEARISH"],
    current_bar_index: int,
    ismt_signals: Sequence[IsmtSignal],
    smt_signals: Sequence[SmtSignal],
) -> IsmtSignal | SmtSignal | None: ...
```

**Direction mapping:**
- `BULLISH` ↔ bullish pattern / long-side structure: accept signals where the signal’s trading direction is **long** — if your Phase 4 dataclasses use `LONG`/`SHORT`, map `BULLISH→LONG`, `BEARISH→SHORT` **inside this function only** (document mapping table in docstring).

**Window (D-05):** Keep iff `current_bar_index - 4 <= signal.confirmed_at <= current_bar_index`.

**Invalidation (D-06):** Drop signals with `signal.invalidated is True`.

**Selection (D-04):** Among survivors, choose **maximum** `confirmed_at`. **Tie on `confirmed_at`:** choose **`SMT` over `ISMT`** using `signal_kind` / `isinstance` rule — **freeze in tests** (`test_sig_tiebreak_prefers_smt`).

**SIG-03 helper:**

```python
def signal_source_value(sig: IsmtSignal | SmtSignal) -> int:
    # ISMT => 1, SMT => 0
```

**Invalidation maintenance:** If not already implemented in 04-01/04-02, add `apply_structure_invalidations(signals, bars_slice)` in `entry.py` **or** call into `ismt.py`/`smt.py` helpers — must mark `invalidated=True` when price **trades back through** defining swing level (bearish ISMT: **close > SH1.price** after signal). This plan **must** make `get_structural_confirmation` respect the flag even if emitters set it elsewhere.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement get_structural_confirmation + signal_source_value</name>
  <read_first>
    - `src/entry.py` (existing `EntryEngine.get_structural_confirmation` — refactor to module-level function **or** thin wrapper; remove ISMT-first sort)
    - `src/ismt.py` (`IsmtSignal`)
    - `src/smt.py` (`SmtSignal`)
    - `.planning/phases/04-signal-generation-ismt-smt-entry/04-CONTEXT.md` (D-04–D-06)
  </read_first>
  <acceptance_criteria>
    - `grep -n "def get_structural_confirmation" src/entry.py` exists with `direction` as **first** parameter after refactor.
    - `grep -n "signal_source_value" src/entry.py` OR adjacent utility module.
    - No comment or code path stating “ISMT takes priority” / “check ISMT first” — **grep** `ISMT first` returns empty in `src/entry.py`.
  </acceptance_criteria>
  <action>
    Replace legacy selection that sorted only by time without direction or used wrong window start (`current_idx - entry_signal_lookback` must equal **4** back for 5-bar inclusive window — verify constant: `window_low = i - 4`).

    Ensure **lists may contain mixed types** — use `typing.Union` / `isinstance` for tie-break.

    Keep `EntryEngine` methods delegating to free function if helpful for tests.

    **Do not** change 3-Step / Aggressive logic yet beyond what’s required to compile (stub calls if needed — minimal edits confined to structural selection helpers to avoid breaking imports).
  </action>
  <verify>
    <automated>python -c "import importlib; importlib.import_module('src.entry')"</automated>
  </verify>
  <done>Module imports; structural function present per acceptance grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/phase04/test_structural_confirmation.py</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` (SIG-01 wording — note superseded)
    - `src/entry.py`
    - `tests/phase04/conftest.py`
  </read_first>
  <behavior>
    - `test_sig_equal_priority_smt_wins_when_more_recent`: ISMT `confirmed_at=244`, SMT `confirmed_at=245`, `i=247` → pick SMT; `signal_source_value==0`.
    - `test_sig_rejects_confirmed_at_i_minus_6`: signal at `i-6` → `None`.
    - `test_sig_drops_invalidated`: higher `confirmed_at` but `invalidated=True`, older valid wins.
    - `test_sig_tiebreak_prefers_smt`: same `confirmed_at`, one ISMT one SMT → SMT returned (documents deterministic tie).
    - `test_sig_filters_by_direction`: bullish signal must not satisfy `BEARISH` query.
    - `test_sig_signal_source_ismt_is_1`: `signal_source_value(ismt)==1`.
  </behavior>
  <action>
    Create `tests/phase04/test_structural_confirmation.py` constructing **minimal fake** `IsmtSignal`/`SmtSignal` instances (dataclasses allow `confirmed_at`, `invalidated`, `signal_kind`, direction fields).

    Explicit test name `test_sig_req_sig02_superseded_by_context_d04` optional alias documenting governance.

    Run `python -m pytest tests/phase04/test_structural_confirmation.py -q --tb=short`.
  </action>
  <verify>
    <automated>python -m pytest tests/phase04/test_structural_confirmation.py -q --tb=short</automated>
  </verify>
  <done>All behaviors green; documents D-04 over ROADMAP success criterion #4 / REQ SIG-02.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/phase04/test_structural_confirmation.py -q`
</verification>

<success_criteria>
- Selector matches D-04–D-06 and exposes SIG-03 encoding.
- At least one test proves SMT can beat ISMT on recency (contradicts naive REQ ordering).
</success_criteria>

<output>
After completion, create `.planning/phases/04-signal-generation-ismt-smt-entry/04-03-SUMMARY.md`
</output>
